{
  "name": "sample_janken",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "firebase": "^7.19.0",
    "node-sass": "^4.14.1",
    "nuxt": "^2.13.3",
    "sass-loader": "^9.0.3",
    "vuex-persistedstate": "^3.1.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
